# Server Configuration
PORT=3001
NODE_ENV=development

# ============================================
# Database Configuration
# ============================================
# Du kannst ENTWEDER einzelne Felder ODER eine Connection URL verwenden.
# Einzelne Felder haben Vorrang vor der URL wenn beide angegeben sind.

# Option 1: Einzelne Felder (empfohlen für existierenden MySQL Server)
DB_HOST=localhost
DB_PORT=3306
DB_USER=gamepanel
DB_PASSWORD=gamepanel
DB_NAME=gamepanel

# Option 2: Connection URL (alternativ)
# DATABASE_URL=mysql://gamepanel:gamepanel@localhost:3306/gamepanel

# ============================================
# JWT Secret (generate a strong random string!)
# ============================================
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ============================================
# CORS (comma-separated list of allowed origins)
# ============================================
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ============================================
# Backend URL (öffentliche URL für Agent-Scripts)
# ============================================
BACKEND_URL=http://localhost:3001
